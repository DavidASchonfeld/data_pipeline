Flask==2.3.3
gunicorn==22.0.0
python-dotenv==1.0.1

# Dash — data visualization framework built on Flask + Plotly
dash==2.17.1
plotly==5.22.0

# Data + database
pandas==2.2.2
SQLAlchemy==2.0.30    # 2.x required — uses text() wrapper for raw SQL
pymysql==1.1.1        # MariaDB/MySQL driver for SQLAlchemy

# Snowflake dependencies
snowflake-connector-python==3.10.1
snowflake-sqlalchemy==1.6.1       # 1.6.x+ required for SQLAlchemy 2.x compatibility

# Security extensions
flask-limiter==3.5.0    # per-IP rate limiting — prevents Snowflake query flooding
flask-talisman==1.1.0   # HTTP security headers: CSP, X-Frame-Options, X-Content-Type-Options
flask-cors==4.0.1       # CORS policy — restricts cross-origin requests to known origins

# Spot instance interruption polling
requests>=2.31.0        # HTTP client for EC2 IMDS spot termination check (also a transitive dep of dash)
