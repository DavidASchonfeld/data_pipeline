apiVersion: v1
kind: PersistentVolume
metadata:
  name: dag-pv
  namespace: airflow-my-namespace
spec:
  accessModes:
    - ReadWriteOnce  # ReadOnlyMany
  capacity:
    storage: 1Gi
  hostPath:
    path: /home/ubuntu/airflow/dags  # updated path to match deploy.sh sync destination
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual  # "manual" opts out of the local-path provisioner so it never adds nodeAffinity
  claimRef:
    namespace: airflow-my-namespace
    name: dag-pvc