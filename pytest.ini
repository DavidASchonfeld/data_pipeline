[pytest]
testpaths = tests genai
markers =
    offline: no network or API key required — runs on every deploy
    live: requires a real API key — run manually only
addopts = -v --no-header --strict-markers
filterwarnings =
    ignore:You seem to already have a custom sys.excepthook:RuntimeWarning
